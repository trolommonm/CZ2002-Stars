package model;

/**
 * Represents the different account types that can access the Stars Application. The pre-defined group of constants
 * under AccountType are: ADMIN and STUDENT since there are only these two groups of users that can log in to the
 * program.
 */
public enum AccountType {
    ADMIN,
    STUDENT
}
